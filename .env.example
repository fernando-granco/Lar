# Copy to .env and adjust. Everything is optional.

# Your timezone, so "today" and due dates line up with your household.
TZ=America/New_York

# Optional API key. When set, agents and scripts must send it as
#   Authorization: Bearer <key>   or   X-Api-Key: <key>
# The browser app on your LAN keeps working without it.
LAR_API_KEY=
