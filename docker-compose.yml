services:
  lar:
    build: .
    image: lar:latest
    container_name: lar
    ports:
      - "3001:3000"
    volumes:
      - lar-data:/app/data
    env_file:
      - path: .env
        required: false
    environment:
      - TZ=${TZ:-UTC}
    restart: unless-stopped

volumes:
  lar-data:
